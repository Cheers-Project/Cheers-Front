import React from 'react';
import styled from 'styled-components';

const Spacer = () => {
  return <SpacerWrapper />;
};

const SpacerWrapper = styled.div`
  height: 11rem;
`;

export default Spacer;
